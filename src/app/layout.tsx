import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CloudTask FAQ Assistant — NLP Chatbot",
  description: "An FAQ chatbot that matches user questions to FAQ entries using TF-IDF and cosine similarity. Built with Next.js 16 and a pure-TypeScript NLP pipeline.",
  keywords: ["FAQ chatbot", "NLP", "TF-IDF", "cosine similarity", "Next.js", "TypeScript"],
  authors: [{ name: "Z.ai Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "CloudTask FAQ Assistant",
    description: "NLP-powered FAQ chatbot with TF-IDF + cosine similarity matching",
    url: "https://chat.z.ai",
    siteName: "Z.ai",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "CloudTask FAQ Assistant",
    description: "NLP-powered FAQ chatbot with TF-IDF + cosine similarity matching",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider>
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
